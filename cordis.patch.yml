# dsh-qol 的 bundle 激活补丁。
# 本包通过 `dsh plugin --profile web add github:...` 安装后，dsh 会把它
# 加入该 profile 的 dsh.profile.bundles，并在启动时应用本补丁层。
- insert:
    - id: dsh-qol
      name: dsh-qol
      config:
        enabled: true
